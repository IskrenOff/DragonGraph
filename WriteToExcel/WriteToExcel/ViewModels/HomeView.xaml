<UserControl x:Class="WriteToExcel.ViewModels.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WriteToExcel.ViewModels"
             
             mc:Ignorable="d" 
             d:DesignHeight="650" d:DesignWidth="1050">
    <Grid>
        

        <!-- File path field -->
        <TextBox x:Name="excelFilePathCell"  FontSize="18" FontFamily="Georgia" Background="{StaticResource titleColor3}" 
                 Grid.Row="0" Width="Auto" Height="20"
                 HorizontalAlignment="Stretch"  Margin="200,0,100,0"/>
        <!--Text description for the field-->
        <TextBox FontSize="20" TextWrapping="Wrap" TextAlignment="Center" Text="File path" Foreground="{StaticResource plainTextColor1}"
                 Margin="90" HorizontalAlignment="Left" Height="25" Width="100" FontFamily="Georgia" FontWeight="Medium" Background="Transparent" BorderThickness="0"/>

    </Grid>
</UserControl>
